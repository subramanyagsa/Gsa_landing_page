import React, { useState, useEffect } from 'react'; import { Button } from "@/components/ui/button"; import GridPattern from './GridPattern'; import ConsultationDialog from './ConsultationDialog'; import { cn } from '@/lib/utils'; const HeroSection = () => { const [isMounted, setIsMounted] = useState(false); useEffect(() => { const timeout = setTimeout(() => setIsMounted(true), 100); return () => clearTimeout(timeout); }, []); return ( <section className="relative w-full h-[90vh] min-h-[700px] flex items-center justify-center text-center overflow-hidden"> <GridPattern /> <div className="absolute inset-0 bg-gradient-to-b from-transparent to-background"></div> <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[50vw] h-[50vh] bg-primary/20 rounded-full blur-[150px] animate-pulse z-0" /> <div className="container px-4 md:px-6 relative z-10"> <div className="max-w-4xl mx-auto space-y-6"> <h1 className={cn( "text-3xl md:text-4xl font-bold tracking-tighter leading-tight transition-all duration-1000 ease-out", isMounted ? "opacity-100 translate-y-0" : "opacity-0 translate-y-4" )}> We Take Care of Your Finances. <br />So you can <span className="text-primary">Take care of Growth .</span> </h1> <p className={cn( "text-lg md:text-xl text-muted-foreground max-w-3xl mx-auto transition-all duration-1000 ease-out", isMounted ? "opacity-100 translate-y-0 delay-200" : "opacity-0 translate-y-4" )}> End-to-end accounting, tax, and payroll solutions that free your time, cut costs, and maximize profits. </p> <div className={cn( "flex items-center justify-center transition-all duration-1000 ease-out", isMounted ? "opacity-100 translate-y-0 delay-300" : "opacity-0 translate-y-4" )}> <ConsultationDialog> <Button size="lg" className="text-lg px-8 py-6 rounded-full font-semibold shadow-lg transition-all duration-300 hover:shadow-primary/50 hover:scale-105"> Get a Free Consultation </Button> </ConsultationDialog> </div> </div> </div> </section> ); };export default HeroSection;